# This is Git's per-user configuration file.

[user]
	name = daryn
	email = daryn@hex50.com

[core]
    editor = code --wait

[alias]
	graph = log --oneline --decorate --graph --all
	
[commit]
    template = ~/.gitmessage
